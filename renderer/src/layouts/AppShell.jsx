/**
 * App shell placeholder for HashRouter takeover (Phase 3–4).
 * Vanilla #app sidebar/pages remain authoritative until page slices migrate.
 */
export function AppShell({ children }) {
  return <div className="react-app-shell">{children}</div>;
}
